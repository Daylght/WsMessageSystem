server:
  port: 8888
spring:
  application:
    name: WsMessageSystem
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/WsMS?serverTimezone=UTC
mybatis:
  mapper-locations: classpath:mappers/*xml